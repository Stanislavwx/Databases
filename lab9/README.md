# 📘 README — Лабораторна робота №9
### Робота з контейнеризованими базами даних. Active Record та DAO в Python

## Мета роботи
- Розгортання PostgreSQL у Docker-контейнері.
- Перемикання між локальною та контейнерною БД.
- Реалізація шаблонів доступу до даних: Active Record та DAO.
- Порівняння продуктивності SELECT-запитів.

## Структура проєкту
```
lab9/
│── lab9_client.py
│── docker-compose.yml
│── README.md
└── venv/
```

## Розгортання PostgreSQL у Docker
```
docker-compose up -d
docker ps
```

## Підготовка Python-середовища
```
python -m venv venv
venv\Scripts\activate    # Windows
source venv/bin/activate  # Linux/Mac
pip install psycopg2-binary
```

## Запуск програми
```
python lab9_client.py
```

При запуску програма пропонує вибрати джерело БД:
- 1 — локальна PostgreSQL
- 2 — контейнерна PostgreSQL

## Функції програми

### Active Record
1 — Показати всіх клієнтів  
2 — Додати клієнта  
3 — Оновити клієнта  
4 — Видалити клієнта  

### DAO
5 — Показати всіх клієнтів  
6 — Додати клієнта  
7 — Оновити клієнта  
8 — Видалити клієнта  

### Вимірювання продуктивності
9 — вимірювання часу SELECT для локальної та контейнерної БД.

## Висновки
Було розгорнуто PostgreSQL у Docker, реалізовано доступ до БД за допомогою шаблонів Active Record і DAO, а також проведено аналіз продуктивності SELECT-запитів.

## Зупинка контейнера
```
docker-compose down
docker-compose down -v   # з видаленням даних
```
